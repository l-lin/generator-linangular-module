language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-linangular-module' ]; then cd .. && eval "mv $currentfolder generator-linangular-module" && cd generator-linangular-module; fi

